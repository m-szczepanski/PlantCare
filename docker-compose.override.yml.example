# Local development overrides.
# Copy to docker-compose.override.yml (auto-loaded by docker compose) and tweak.
services:
  api:
    restart: unless-stopped

  web:
    restart: unless-stopped

  ntfy:
    restart: unless-stopped
